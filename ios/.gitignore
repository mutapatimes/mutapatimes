App/build
App/Pods
App/output
App/App/public
DerivedData
xcuserdata

# NOTE: capacitor-cordova-ios-plugins is committed so Xcode Cloud can resolve
# its local Swift packages even before `npx cap sync ios` runs.

# Generated Config files
App/App/capacitor.config.json
App/App/config.xml
